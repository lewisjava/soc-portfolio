---
title: Tempest
platform: TryHackMe
difficulty: Medium
date: 2026-08-04
tags: [Triage, Challenge]
summary: You are tasked to conduct an investigation from a workstation affected by a full attack chain
---

# Tempest

Write your content in standard markdown below. Everything here gets rendered.

Scenario:
Tempest Incident

In this incident, you will act as an Incident Responder from an alert triaged by one of your Security Operations Center analysts. The analyst has confirmed that the alert has a CRITICAL severity that needs further investigation.

As reported by the SOC analyst, the intrusion started from a malicious document. In addition, the analyst compiled the essential information generated by the alert as listed below:

    The malicious document has a .doc extension.
    The user downloaded the malicious document via chrome.exe.
    The malicious document then executed a chain of commands to attain code execution.

Workflow:
Start with the events generated by sysmon.
EvtxEcmd, Timeline Explorer, and SysmonView can interpret sysmon logs.
Follow the child processes of WinWord.exe.
Use filters such as ParentProcessID or ProcessID to correlate the relationship of each process.
We can focus on sysmon events such as Process Creation (Event ID 1) and DNS Queries (Event ID 22) to correlate the activity generated by the malicious document.

## Initial Access - Malicious document
**the user of this machine was compromised by a malicious document. what is the file name of the document**
Using sysmon view I can see that the user benimaru downloaded a file called free_magicules.doc at 5:12:58 PM
![tempest](/static/images/tempest.png)

**What is the name of the compromised user and machine**
From sysmon view I can see the compromised user is Benimaru and since I am on the compromised host I just ran hostname in powershell to fine the machine name
![tempest1](/static/images/tempest1.png)

**What is the PID of the microsoft word process that opened the malicious document**
using timeline explorer and filtering for the document name I was able to find all events associated with it and the one that opened the malicious document has a PID of 496 this occured at 17:13:34 PM

**Based on sysmon logs what is the IPv4 address resolved by the malicious domain used in the previous question**
Going over to wireshark I was able to find the HTTP stream from the name of the downloaded doc which gave me me the IP and name of the malicious domain 
![tempest3](/static/images/tempest3.png)

To correlate the events I went into sysmon view and looked at the events related to WINWORD.EXE where I can see a connection was made to the malicious domain
![tempest4](/static/images/tempest4.png)

**What is the base64 encoded string in the malicious payload executed by the document**
Since I had the PID of the word process opened by the malicious document I was able to use that to search sysmon logs for any child processes spawned by the malicious document, including the one with the base64 encoded string.
![tempest5](/static/images/tempest5.png)
after pasting the base64 string into cyberchef I got the following
![tempest6](/static/images/tempest6.png)

**What is the CVE number of the exploit used by the attacker to acheive a remote code execution**
After doing some research this is an exploit known as "Follina" where the attacker manipulates Microsoft support diagnostic tool protocol to execute their own powershell commands. 
![tempest7](/static/images/tempest7.png)

## Initial access - stage 2 execution
workflow:
The Autostart execution reflects explorer.exe as its parent process ID.
Child processes of explorer.exe within the event timeframe could be significant.
Process Creation (Event ID 1) and File Creation (Event ID 11) succeeding the document execution are worth checking.

**The malicious execution of the payload wrote a file on the system, what is the full target path of the payload**
Since I have the name of the malicious file from the decoded base64 string as well as the partial path I can search it in syslogs and find the full target path. at this point the file is extracted and is called update.lnk
![tempest8](/static/images/tempest8.png)

**The implanted payload executes once the user logs into the machine what is the executed command upon a successfull login of the compromised user**
Looking at sysmon viewer I could see that two explorer.exe processes started on boot up, using both their process ID's I looked for any child processes spawned by them. The process ID 5784 for explorer.exe contained a windows powershell command using certutil to download a file called first.exe into Public\Downloads at 17:15:11
![tempest9](/static/images/tempest9.png)

**Based on sysmon logs, what is the SHA256 hash of the maliciosu binary downloaded for stage 2 execution**
searching the executable shows me the sha256 hash of the file
![tempest10](/static/images/tempest10.png)

**Th stage 2 payload downloaded establishes a connection to a c2 server. what is the domain and port used by the attacker**
Using sysmon view I can see the connection establishes to resolvecyber.xyz:80

## Initial access - Malicious document traffic
workflow:
Since we have discovered network-related artefacts, we may again refer to our cheatsheet, which focuses on Network Log Analysis:

    We can now use Brim and Wireshark to investigate the packet capture.
    Find network events related to the harvested domains and IP addresses.
    Sample Brim filter that you can use for this investigation: _path=="http" "<malicious domain>"

**What is the URL of the malicious payload embedded in the document**
Using brim I put the two different malicious urls in and had a look through what was returned. With phishteam.xyz I can see that /index.html is being requested by microsoft office, likely making this the url in the malicious document.
![tempest11](/static/images/tempest11.png)

**What is the encoding used by the attacker on the c2 connection**
The next step here was to go to wireshark and filter for both the compromised host IP and the IP for th domain cyberresolver.xyz and look at the conversations. By doing this I can see there is some base64 encoding in some of the GET requests.
![tempest12](/static/images/tempest12.png)

**The malicious c2 binary sends a payload using a parameter that contains the executed command results, what is the parameter used by the binary**
Looking at the previous screenshot the parameter q can be seen.

**The malicious c2 binary connects to a specific url to get the command to be executed. what is the URL used by the binary**
Again in the previous screenshot the specific URL can be seen /9ab62b5

**What is the HTTP command used by the binary**
The command GET is sued and again can be seen in the previous screenshot

**Based on the user agent, what programming language was used by the attacker to compile the binary**
Looking into the TCP stream I can see the user agent and the programming language used was Nim
![tempest13](/static/images/tempest13.png)

## Discovery - Internal Recon
Workflow:
    Find network and process events connecting to the malicious domain.
    Find network events that contain an encoded command.
    We can use Brim to filter all packets containing the encoded string.
    Look for endpoint enumeration commands since the attacker is already inside the machine.

In addition, we may refer to our cheatsheet for Brim to quickly investigate the encoded traffic with the following filters:

To get all http requests related to the malicious c2 traffic: 
```
_path=="http" "<replace domain>" id.resp_p==<replace port> | cut ts, host, id.resp_p, uri | sort ts
```

**The attacker was able to discover a sensitive file inside the machine of the user. What is the password discovered on the file**
Using brim I can get all the encoded commands and paste them into cyberchef to see what was ran
![tempest14](/static/images/tempest14.png)

![tempest15](/static/images/tempest15.png)
Exporting the cyberchef output of all the commands and using ctrl + f to find a password produces the answer
![tempest16](/static/images/tempest16.png)

**The attacker then enumerated the list of listening ports inside the machine, what is the listening port that could provide a remote shell inside the machine**
From the commands ran there are a few listening ports open
![tempest17](/static/images/tempest17.png)
Searching these ports through windows and seeing which are typically used for remote shells
![tempest18](/static/images/tempest18.png)

**The attacker then established a reverse socks proxy to access the internal services hosted inside the machine. what is the command executed by the attacker to establish the connection**
Looking inside the rest of the decoded strings doesn't reveal much except a process called ch.exe looking in the timeline explorer shows a network connection made with this binary at 17:18:50
![tempest19](/static/images/tempest19.png)


**What is the sha256 hash of the binary used by the attacker to establish the reverse socks proxy connection**
![tempest20](/static/images/tempest20.png)

**What is the name of the tool used by the attacker based on the sha256 hash**
Looking up the hash in Malware bazaar shows the tool used is called chisel
![tempest21](/static/images/tempest21.png)


**The attacker then used the harvested credentials from the machine. based on the succeeding process after the execution of the socks proxy what service did the attacker use to authenticate**
the next process create that takes place after the socks proxy creation is at 17:19:06 and is a wsmprovhost binary, a remote powershell host proxy spawned by WinRM the service the attacker used to authenticate.
![tempest22](/static/images/tempest22.png)

## Privilege escalation - Exploiting privileges
Workflow:
focus on the following network and endpoint events:

    Look for events executed after the successful execution of the reverse socks proxy tool.
    Look for potential privilege escalation attempts, as the attacker has already established a persistent low-privilege access.

**After discovering the privileges of the current user, the attacker then downloaded another binary to be used for privilege escalation. what is the name and sha256 hash of the binary**
Looking if whoami has been run in the syslogs shows the command was ran three times by benimaru at 17:15:25 at the earliest
![tempest](/static/images/tempest23.png)
So I went to Event viewer and filtered for event ID 11 and looked at files created after this, using find "benimaru" for files created by this user shows a file called spf.exe was downloaded at 17:20:10, only 5 minutes later
![tempest24](/static/images/tempest24.png)
Going back to timeline explorer with this info I can now search for spf.exe and get the sha256 hash of the file to answer the question.
![tempest25](/static/images/tempest25.png)

**Based on the sha256 hash of the binary what is the name of the tool used**
Going back to malware bazaar with the hash shows the tool used is called printspoofer, which is rather self explanatory.
![tempest26](/static/images/tempest26.png)

**This tool exploits a specific privilege owned by the user what is the name of the privilege**
Researching the tool shows it abuses SeImpersonatePrivilege via the print spooler service
![tempest27](/static/images/tempest27.png)

**Then, the attacker executed the tool with another binary to establish a c2 connection, what is the name of the binary**
 Going back to timeline viewer and searching the binary shows it executed with another binary called final.exe
![tempest](/static/images/tempest28.png)

**The binary connects to a different port from the first c2 connection, what is the port used**
looking at final.exe binary in sysmon view shows it connects to an ip 161.71.222.162:8080 which resolves to resolvecyber.xyz still

## Actions on objective - Fully-owned Machine
workflow:
investigate events after a successful privilege escalation:

    Useful Brim filter to get all requests related to the malicious traffic : _path=="http" "<replace domain>" id.resp_p==<replace port> | cut ts, host, id.resp_p, uri | sort ts
    The attacker gained SYSTEM privileges; now, the user context for each malicious execution blends with NT Authority\System.
    All child events of the new malicious binary used for are worth checking.

**Upon achieving SYSTEM access, the attacker then created two users. What are the account names**
Searching net.exe in timelineviewer will show me what users were created and what groups they were also added too, the two users created were shion and shuna. at 17:27:19
![tempest29](/static/images/tempest29.png)

**prior to the successful creation of the accounts, the attacker executed commands that failed in the creation attempt. What is the missing option that made the attempt fail**
This is shown in the previous screenshot the attacker forgot "/add"

**Based on windows event logs, the accounts were successfully created, what is the event ID that indicates the account creation activity**
Windows security event ID 4720

**The attacker added one of the accounts in the local admins group, what is the command used by the attacker**
net localgroup administrators /add shion
![tempest30](/static/images/tempest30.png)

**Based on windows event logs, the account was successfully added to a sensitive group, what is the event ID that indicates the addition to a sensitive local group**
4720 is the windows ID for members being added to a security-enabled local group

**After account creation, the attacker executed a technique to establish persistence administrative access. what is the command executed by the attacker to achieve this**
It is likely the attacker will use sc.exe to create a scheduled task but simply searching final.exe since that is the binary they want to use for persistence I can find any commands ran against it where I find that it was added to sc.exe to auto start. this task was created at 17:26:29 ![tempest32](/static/images/tempest31.png)
---

- **Tempest**
- **TryHackMe**
- **Medium**
- **2026-08-04**
- **Triage, SIEM, challenge**
- **A full chain of attack write up from initial access to actions on objectives**

